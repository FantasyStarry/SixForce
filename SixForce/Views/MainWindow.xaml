<Window x:Class="SixForce.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="DST德森特标定软件" 
        Height="720" Width="1200"
        MinHeight="600" MinWidth="960"
        Icon="/Assets/Images/favicon.ico"
        Background="{DynamicResource ThemeBackgroundBrush}"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <LinearGradientBrush x:Key="HeaderGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="{DynamicResource ThemePrimaryColor}" Offset="0"/>
            <GradientStop Color="{DynamicResource ThemePrimaryDarkColor}" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部导航栏 -->
        <Border Grid.Row="0" 
                Background="{DynamicResource ThemeHeaderBackgroundBrush}"
                Padding="20,12">
            <Border.Effect>
                <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.05" Direction="270"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 左侧Logo和标题 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="36" Height="36" CornerRadius="8" Background="{StaticResource HeaderGradient}">
                        <TextBlock Text="六" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="DST德森特标定软件" 
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource ThemeTextPrimaryBrush}"/>
                        <TextBlock Text="六分力传感器标定系统" 
                                   FontSize="11"
                                   Foreground="{DynamicResource ThemeTextMutedBrush}"/>
                    </StackPanel>
                </StackPanel>

                <!-- 中间产品选择 -->
                <StackPanel Grid.Column="1" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                    <TextBlock Text="变速器型号" 
                               FontSize="13"
                               Foreground="{DynamicResource ThemeTextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,10,0"/>
                    <ComboBox Width="180"
                              Height="34"
                              FontSize="13"
                              ItemsSource="{Binding Products}"
                              SelectedItem="{Binding SelectedProduct}"/>
                </StackPanel>

                <!-- 主题切换 -->
                <StackPanel Grid.Column="2" 
                            Orientation="Horizontal" 
                            VerticalAlignment="Center"
                            Margin="0,0,16,0">
                    <TextBlock Text="主题" 
                               FontSize="12"
                               Foreground="{DynamicResource ThemeTextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"/>
                    <ToggleButton x:Name="ThemeToggle"
                                  Style="{StaticResource ThemeToggleButton}"
                                  Click="ThemeToggle_Click"/>
                </StackPanel>

                <!-- 右侧状态 -->
                <Border Grid.Column="3" 
                        Background="{DynamicResource ThemePrimaryLightBrush}" 
                        CornerRadius="14" 
                        Padding="10,5"
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="6" Height="6" Fill="{DynamicResource ThemeSuccessBrush}" Margin="0,0,6,0"/>
                        <TextBlock Text="系统就绪" FontSize="12" Foreground="{DynamicResource ThemeTextPrimaryBrush}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- 主内容区域 -->
        <Grid Grid.Row="1" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" MinWidth="180"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="280" MinWidth="250"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧面板 -->
            <Border x:Name="LeftArea" 
                    Grid.Column="0" 
                    Style="{StaticResource CardStyle}"
                    Margin="0,0,8,0"/>

            <!-- 中间面板 -->
            <Border x:Name="CenterArea" 
                    Grid.Column="1" 
                    Style="{StaticResource CardStyle}"
                    Margin="8,0"/>

            <!-- 右侧面板 -->
            <Border x:Name="RightArea"
                    Grid.Column="2" 
                    Style="{StaticResource CardStyle}"
                    Margin="8,0,0,0"/>
        </Grid>
    </Grid>
</Window>